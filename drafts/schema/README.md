# JSON Schema for the Sync Media Narration data format (DRAFT!)

Folder contents:

* `sync-media-narration.schema.json` is the JSON Schema, conforming to "draft-v7".
* `sync-media-narration.sample.json` is a Sync Media Narration JSON sample, which validates against the JSON Schema.

A selection of online tools to try the above JSON Schema and JSON sample:

* https://jsonschemalint.com/#/version/draft-07/markup/json
* https://www.jsonschemavalidator.net
